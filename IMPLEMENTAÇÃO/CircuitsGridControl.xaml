<UserControl x:Class="ProjetoFBD.CircuitsGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <DataGrid x:Name="Grid"
                  ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  AutoGenerateColumns="False" 
                  HeadersVisibility="Column"
                  FontFamily="Segoe UI Emoji"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=ID_Circuito}" Header="ID" IsReadOnly="True" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Path=Nome}" Header="Name" Width="*"/>
                <DataGridTextColumn Binding="{Binding Path=Cidade}" Header="City" Width="*"/>
                <DataGridTextColumn Binding="{Binding Path=Pais}" Header="Country" Width="*"/>
                <DataGridTextColumn Binding="{Binding Path=Comprimento_km}" Header="Length (km)" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Path=NumCurvas}" Header="Num Corners" Width="Auto"/>
                <DataGridTemplateColumn Header="Map" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Ver Mapa" Padding="6,2" Margin="4,1"
                                    Click="MapButton_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>